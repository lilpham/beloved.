<div class="field">
	<%= form_for @users, html: { multipart: true } do |f| %>

	  <%= file_field_tag "images[]", type: :file, multiple: true %>
	  
	<div class="actions">
	  <%= f.submit "Add Photo" %>
	 </div>
	<% end %>
	<br>
</div>