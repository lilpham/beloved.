

<%= link_to "Check List", user_tasks_url(current_user.name) %>

<br>

<%= link_to "Budgeter", user_expenses_url(current_user.name) %> 
<br>

<%= link_to "Guest List", user_guests_url(current_user.name) %> 

<br>

<%= link_to "Treat", user_treat_path(current_user.name) %>

<br>

<%= link_to "Create", user_create_path(current_user.name) %>

<br>

<%= link_to "Settings", user_settings_path(current_user.name) %>

<br>

